services:
  backend:
    build: ./backend
    container_name: expense-backend
    ports:
      - "8888:8081"
    environment:
      - DB_URL=jdbc:mysql://yamabiko.proxy.rlwy.net:42983/railway?allowPublicKeyRetrieval=true&useSSL=true
      - DB_USERNAME=root
      - DB_PASSWORD=aEQWlyFuNDxgGItPixpctIPjIyxSTvYC
    # Đã bỏ depends_on vì dùng DB trên mây

  frontend:
    build:
      context: ./frontend
      args:
        - VITE_API_URL=http://localhost:8888/api
    container_name: expense-frontend
    ports:
      - "8085:80"
    depends_on:
      - backend
